import React from 'react';
import './styles/init.css';

export const Instagram = () => {
    return (
        <section className = 'header'>
            <div className = 'logo' />
        </section>
    );
};
